# Notes index

Newest first. Protocol: `skills/vrc-dcc/notes.md`.

**Door (new window):** gitignored `CURRENT.md` (改模). Station self-maintain: gitignored `HANDOFF.md` + `tracks/` (templates in `templates/HANDOFF.md`). Dated `notes/YYYY-MM-DD-*.md` are gitignored on this public skeleton — they belong to the clone-owner overlay.

Agents still write those files locally after a job (`templates/AFTER_ACTION.md`). This committed file is the **schema**, not an archive of one body's history.

| Date | File | Status | Topic |
|---|---|---|---|
| — | (local overlay) | — | Clone-owner dated notes stay gitignored |
