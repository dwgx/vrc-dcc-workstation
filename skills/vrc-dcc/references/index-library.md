# Index library (load on 改模 find / Booth / dispatch)

Full playbook: [`maps/AGENT.md`](../../../maps/AGENT.md) · catalog [`maps/INDEX.md`](../../../maps/INDEX.md) · graphs [`maps/GRAPHS.md`](../../../maps/GRAPHS.md)

```
python maps/query.py <avatar> <words>
python maps/query.py library <words>
python maps/library/scan.py
python maps/booth.py --search <name>
powershell -File .\scripts\graphs-ready.ps1
```

On-body: `query.py <avatar>` / `MAP.md`. Owned USB packs: `query.py library` / `LIBRARY.md`. C#: `codegraph_explore` + `projectPath` (GRAPHS.md). Booth last. Do not walk the USB root every chat. Envelope in AGENT.md.
