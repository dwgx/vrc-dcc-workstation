# Avatar Unity project — pointer

This folder is the **Unity product** (scenes, prefabs, VPM). It is not the DCC workstation.

Workstation clone (contract, skills, `notes/` memory): set in that clone's gitignored `local.json`, or the path the owner named.

## Job init

A VRChat / avatar / DCC ask **is** this init. Classify by intent (see station `templates/JOB.md`). Do not require a passphrase.

1. Read the station `notes/CURRENT.md` (door). Then one playbook for the named slice. `INDEX.md` is the archive.
2. Do **not** run the station install questionnaire (`templates/INIT_QUESTIONNAIRE.md`).
3. Do not treat the station clone as this workspace. Do not write the avatar tree from a home/control-plane window.
4. Fingerprints for avatar edits: station `notes/` (**when / why / what**). This Unity tree may have no git.
5. `unityMCP` in this chat before mutating. Never user-global MCP. Human clicks SDK Publish. Official Unity 6 MCP stays off 2022.3.
6. C# / MA source: station `maps/GRAPHS.md`. Missing avatar C# index: `powershell -File <station>/scripts/install-avatar-codegraph.ps1` **in this window**. Home must not run it.

If Cursor still runs `session-probe`: quote it, then stop the four-runtime estate read-down. This job is DCC, not origin.
