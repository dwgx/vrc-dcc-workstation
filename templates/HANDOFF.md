# Handoff (clone-owner overlay)

Copy to gitignored **`notes/HANDOFF.md`**. Present tense. One incomplete next action. Not `CURRENT.md` (that is the 改模 door). Not public git.

When missing: copy this file. Playbook: `skills/vrc-dcc/references/maintain-loop.md`.

```yaml
updated: YYYY-MM-DD
actor: runtime / window
mission: one sentence
status: observed | in_progress | blocked
```

## Exact next action

One command or one file. Not a backlog dump.

## Recovery evidence

- Current writer, owned paths, checked time and Git HEAD/status (or explicit no-Git state).
- Last confirmed tool result and exact evidence paths; keep uncertain/unfinished calls explicit.
- Baseline and backup/restore location for a non-Git product, recorded by its authorized writer.
- Completed acceptance checks, remaining checks, and any evidence made stale by later edits.
- Active web task/dispatch, producer, task-card and STATUS paths, bundle root,
  artifact class, owned write set and conversation URL, if applicable.

After compaction read these pointers, verify present state and continue the next
action. Use targeted history only when a decision is missing. Do not replay a
completed mutation or send because its full transcript is no longer in context.

## Do not

- Handshake a shop body unless the owner named a 改模 job.
- POST `8080` from station. Start CoplayDev on a foreign Editor. SDK Publish.
- Commit `OWNER.md`, `local.json`, `notes/CURRENT.md`, `notes/HANDOFF.md`, `notes/tracks/`, dated notes, live `maps/<id>/`.
- Skip to live `world_*` or a second Unity MCP.

## Tracks

One file per lane under gitignored `notes/tracks/` (copy `templates/TRACK.md`). Link them here. Close or split a track instead of growing this board.

| Track | File | Next |
|---|---|---|
| (example) | `notes/tracks/station.md` | … |

## Uncommitted (this clone)

`git status` is truth. List only what the next window must know.

## After a slice

Rewrite **this file** in present tense. Dated evidence stays `notes/YYYY-MM-DD-*.md`. Standing rules go to a skill or `AGENTS.md`, not a second constitution here.
