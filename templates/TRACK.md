# Track (clone-owner overlay)

Copy to gitignored **`notes/tracks/<id>.md`**. One expansion or maintain lane. Link it from `notes/HANDOFF.md`. Not public git.

```yaml
id: short-id
updated: YYYY-MM-DD
status: later | ready | in_progress | blocked | landed
keep_out_of_git: overlay / secrets / live maps / Unity tree
```

## What this lane is

One sentence.

## Exact next (this lane)

One action. If blocked, say what evidence is missing.

## Keep out

Public skeleton, upload APIs, live Editor from station, copying another body's POLICY/freeze.
