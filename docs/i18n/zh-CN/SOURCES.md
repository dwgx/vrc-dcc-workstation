# 公开骨架存什么

英文规范：[SOURCES.md](../../SOURCES.md)。

**不要**把海量底模、Booth 包、USB 货架、世界 15 张贴图打进 git。那些在 overlay / Unity 工程 / 硬盘货架。公开仓库只存：任意点名角色的握手、身份失败即停、改模规则、命名 `vrc_*`、迭代循环。

网页 / Astra 可以继续搜。进本树的条件：下一只外来角色也会踩到，写成我们的 playbook，并引用原 URL。不要把第三方整个 `SKILL.md` 或研究 ZIP 提交进来。

lilycalInventory / FaceEmo 只在**这只角色已经装了**时当检查路径，不是本站默认衣服方案（默认仍是 Modular Avatar 的国风店菜单）。证据层级：[evidence-layers.md](../../../skills/vrc-dcc/references/evidence-layers.md)。
